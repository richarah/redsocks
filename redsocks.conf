base {
  log_debug = off;
  log_info = off;
  log = "stderr";
  daemon = on;
  user = redsocks;
  group = redsocks;
  redirector = iptables;
}

redsocks {
  local_ip = 0.0.0.0;
  local_port = 7778;

  type = socks5;
  ip = 127.0.0.1;
  port = 7777;
}
